services:
  django:
    volumes:
      - ./src:/app
    command: python manage.py runserver 0.0.0.0:8000
    env_file:
      - ../app/.env.local
    networks:
      - red_docker_proyecto

  nginx:
    networks:
      - red_docker_proyecto

networks:
  red_docker_proyecto:
    external: true